import "server-only";
import { jsPDF } from "jspdf";
import { getPlots } from "./rajukQuery.service";
import { RAJUK_DB } from "./rajukLayers.service";
import { getValidToken } from "./rajukAuth.service";
import type { RajukPlotFeature } from "@/src/types/rajuk-runtime";
import { drawAdaptivePlotLabels, drawNorthArrow, drawScaleBar, drawScaleText, drawCoordinateGrid, drawPublicationFooter, type GeoExtent } from "./mouzaCartography";

export type MouzaPublicationRequest={mouza:string;jl?:string;layers:"rs"|"ms"|"combined";satellite?:boolean};
export type MouzaPublicationResult={filename:string;contentType:"application/pdf";body:Buffer;meta:{mouza:string;width:number;height:number;zoom:number;resolution:number;crs:string;extent:GeoExtent;tileCount:number;plotCount:number;satellite:boolean}};
type Ring=number[][];type Polygon={rings:Ring[]};type Row={attributes:Record<string,unknown>;geometry?:Polygon};
const W=420,H=297,M=14,TOP=26,BOTTOM=25,DW=W-M*2,DH=H-TOP-BOTTOM,ASPECT=DW/DH,TIMEOUT=15_000,SAT_W=3508,SAT_H=2480,MAX_NEIGHBORS=24;
const txt=(a:Record<string,unknown>,keys:string[])=>{for(const k of keys){const v=String(a[k]??"").trim();if(v)return v;}return "N/A"};
const safe=(v:string)=>v.replace(/[^\p{L}\p{N}]+/gu,"-").replace(/^-+|-+$/g,"")||"mouza";
const isMs=(f:RajukPlotFeature)=>{const a=f.attributes as Record<string,unknown>;return a._layer_source==="ms"||a.plot_kind==="ms"||Boolean(String(a.ms_plot_no??"").trim());};
function extent(features:RajukPlotFeature[]):GeoExtent|null{let xmin=Infinity,ymin=Infinity,xmax=-Infinity,ymax=-Infinity;for(const f of features)for(const r of f.geometry?.rings??[])for(const p of r){const x=Number(p[0]),y=Number(p[1]);if(Number.isFinite(x)&&Number.isFinite(y)){xmin=Math.min(xmin,x);ymin=Math.min(ymin,y);xmax=Math.max(xmax,x);ymax=Math.max(ymax,y);}}return Number.isFinite(xmin)?{xmin,ymin,xmax,ymax}:null;}
function fit(e:GeoExtent):GeoExtent{const cx=(e.xmin+e.xmax)/2,cy=(e.ymin+e.ymax)/2,w=Math.max(e.xmax-e.xmin,1e-12)*1.08,h=Math.max(e.ymax-e.ymin,1e-12)*1.08;const fw=w/h<ASPECT?h*ASPECT:w,fh=w/h<ASPECT?h:w/ASPECT;return{xmin:cx-fw/2,ymin:cy-fh/2,xmax:cx+fw/2,ymax:cy+fh/2};}
function project(lon:number,lat:number,e:GeoExtent):readonly[number,number]{return[M+(lon-e.xmin)/Math.max(e.xmax-e.xmin,1e-12)*DW,TOP+(1-(lat-e.ymin)/Math.max(e.ymax-e.ymin,1e-12))*DH];}
function ring(doc:jsPDF,r:Ring,e:GeoExtent){if(r.length<3)return;const p=r.map(([x,y])=>project(Number(x),Number(y),e));const rel:Array<[number,number]>=[];for(let i=1;i<p.length;i++)rel.push([p[i][0]-p[i-1][0],p[i][1]-p[i-1][1]]);doc.lines(rel,p[0][0],p[0][1],[1,1],"S",true);}
function poly(doc:jsPDF,g:Polygon,e:GeoExtent,kind:"rs"|"ms"|"neighbor"){if(kind==="neighbor"){doc.setDrawColor(110,120,130);doc.setLineWidth(.14);doc.setLineDashPattern([1.2,1],0);for(const r of g.rings??[])ring(doc,r,e);doc.setLineDashPattern([],0);return;}const c=kind==="rs"?[255,230,0]:[0,240,255];doc.setDrawColor(18,22,28);doc.setLineWidth(.55);for(const r of g.rings??[])ring(doc,r,e);doc.setDrawColor(c[0],c[1],c[2]);doc.setLineWidth(.32);if(kind==="ms")doc.setLineDashPattern([1.6,.8],0);for(const r of g.rings??[])ring(doc,r,e);if(kind==="ms")doc.setLineDashPattern([],0);}
function mouzaBoundary(doc:jsPDF,g:Polygon|null,e:GeoExtent){if(!g)return;doc.setDrawColor(30,10,22);doc.setLineWidth(1.15);for(const r of g.rings??[])ring(doc,r,e);doc.setDrawColor(230,0,120);doc.setLineWidth(.88);for(const r of g.rings??[])ring(doc,r,e);}
function ringExtent(rings:Ring[]):GeoExtent|null{let xmin=Infinity,ymin=Infinity,xmax=-Infinity,ymax=-Infinity;for(const r of rings)for(const p of r){const x=Number(p[0]),y=Number(p[1]);if(Number.isFinite(x)&&Number.isFinite(y)){xmin=Math.min(xmin,x);ymin=Math.min(ymin,y);xmax=Math.max(xmax,x);ymax=Math.max(ymax,y);}}return Number.isFinite(xmin)?{xmin,ymin,xmax,ymax}:null;}
async function query<T>(layer:number,params:Record<string,string|number|boolean|undefined>):Promise<T>{const q=new URLSearchParams();for(const[k,v]of Object.entries(params))if(v!==undefined)q.set(k,String(v));q.set("f","json");let r=await fetch(`${RAJUK_DB}/${layer}/query?${q}`,{signal:AbortSignal.timeout(TIMEOUT)});if(r.status===401||r.status===403){q.set("token",await getValidToken("https://masterplan.rajuk.gov.bd/server/rest/services"));r=await fetch(`${RAJUK_DB}/${layer}/query?${q}`,{signal:AbortSignal.timeout(TIMEOUT)});}if(!r.ok)throw new Error(`RAJUK mouza query failed (${r.status})`);return await r.json() as T;}
async function context(mouza:string,jl?:string){const where=`UPPER(mauza) = UPPER('${mouza.replace(/'/g,"''")}')${jl?` AND jl_no='${jl.replace(/'/g,"''")}'`:""}`;const current=await query<{features?:Row[]}>(1,{where,outFields:"mauza,jl_no,m_guid,t_guid,upazila_ps,m_district",returnGeometry:true,resultRecordCount:1});const geometry=current.features?.[0]?.geometry??null;const attributes=current.features?.[0]?.attributes??{};if(!geometry)return{geometry:null,neighbors:[] as Row[],attributes};const e=ringExtent(geometry.rings??[]);if(!e)return{geometry,neighbors:[] as Row[],attributes};const envelope=JSON.stringify({xmin:e.xmin,ymin:e.ymin,xmax:e.xmax,ymax:e.ymax,spatialReference:{wkid:4326}});const data=await query<{features?:Row[]}>(1,{geometry:envelope,geometryType:"esriGeometryEnvelope",inSR:4326,spatialRel:"esriSpatialRelIntersects",outFields:"mauza,jl_no,m_guid,t_guid,upazila_ps,m_district",returnGeometry:true,resultRecordCount:MAX_NEIGHBORS+1});const key=`${mouza.toLowerCase()}|${jl??""}`;return{geometry,attributes,neighbors:(data.features??[]).filter(x=>`${txt(x.attributes,["mauza","mauza_name"]).toLowerCase()}|${txt(x.attributes,["jl_no","jl"])}`!==key).slice(0,MAX_NEIGHBORS)};}
async function satellite(e:GeoExtent):Promise<Buffer>{const q=new URLSearchParams({bbox:`${e.xmin},${e.ymin},${e.xmax},${e.ymax}`,bboxSR:"4326",imageSR:"4326",size:`${SAT_W},${SAT_H}`,format:"jpg",f:"image",dpi:"300",compressionQuality:"78",transparent:"false"});const r=await fetch(`https://server.arcgisonline.com/ArcGIS/rest/services/World_Imagery/MapServer/export?${q}`,{signal:AbortSignal.timeout(TIMEOUT),headers:{accept:"image/jpeg"}});if(!r.ok||!(r.headers.get("content-type")??"").toLowerCase().includes("image/"))throw new Error(`Satellite imagery request failed (${r.status})`);return Buffer.from(await r.arrayBuffer());}
function scale(e:GeoExtent):string{const lat=(e.ymin+e.ymax)/2,phi=lat*Math.PI/180,mPerDeg=111412.84*Math.cos(phi)-93.5*Math.cos(3*phi),ground=Math.max(1,(e.xmax-e.xmin)*mPerDeg),den=Math.round((ground/(DW/1000))/100)*100;return`1:${Math.max(100,den).toLocaleString("en-US")}`;}
function neighborLabels(doc:jsPDF,rows:Row[],e:GeoExtent){doc.setFont("helvetica","bold");doc.setFontSize(5.5);for(const n of rows){if(!n.geometry)continue;const b=ringExtent(n.geometry.rings??[]);if(!b)continue;const name=txt(n.attributes,["mauza","mauza_name"]);if(name==="N/A")continue;const [x,y]=project((b.xmin+b.xmax)/2,(b.ymin+b.ymax)/2,e);doc.setTextColor(255,255,255);doc.setDrawColor(255,255,255);doc.setLineWidth(1.2);doc.text(name,x,y,{align:"center",baseline:"middle",renderingMode:"fillThenStroke"});doc.setTextColor(75,80,85);doc.setLineWidth(.15);doc.text(name,x,y,{align:"center",baseline:"middle"});}}

export async function exportMouzaPublicationPdf(input:MouzaPublicationRequest):Promise<MouzaPublicationResult>{const mouza=input.mouza.trim();if(!mouza)throw new Error("Mouza is required");const all:RajukPlotFeature[]=[];for(let offset=0;;offset+=2000){const page=await getPlots({mouza,jl:input.jl,kind:"all",resultRecordCount:2000,resultOffset:offset});const rows=page.features??[];all.push(...rows);if(rows.length<2000)break;}const features=all.filter(f=>input.layers==="combined"?true:input.layers==="ms"?isMs(f):!isMs(f));if(!features.length)throw new Error(`No plots found for ${mouza}`);const raw=extent(features);if(!raw)throw new Error("No valid plot geometry found");const e=fit(raw);const ctx=await context(mouza,input.jl).catch(()=>({geometry:null,neighbors:[] as Row[],attributes:{} as Record<string,unknown>}));const doc=new jsPDF({orientation:"landscape",unit:"mm",format:[W,H],compress:true,putOnlyUsedFonts:true});doc.setProperties({title:`LandBD — ${mouza} Mouza Map`,subject:"Publication-grade RS/MS cadastral map",creator:"LandBD"});doc.setFillColor(248,249,250);doc.rect(M,TOP,DW,DH,"F");let sat=false;if(input.satellite){try{doc.addImage(await satellite(e),"JPEG",M,TOP,DW,DH,undefined,"FAST");sat=true;}catch(err){console.warn("Satellite backdrop unavailable; continuing with vector map",err);}}drawCoordinateGrid(doc,e,project);for(const n of ctx.neighbors)if(n.geometry)poly(doc,n.geometry,e,"neighbor");neighborLabels(doc,ctx.neighbors,e);for(const f of features)poly(doc,f.geometry,e,isMs(f)?"ms":"rs");drawAdaptivePlotLabels(doc,features,e,project,isMs);mouzaBoundary(doc,ctx.geometry,e);doc.setFont("helvetica","bold");doc.setFontSize(13);doc.setTextColor(20,30,40);doc.text(`LandBD — ${mouza}`,M,9);const sample=(features[0]?.attributes??{}) as Record<string,unknown>;const jl= input.jl??txt(ctx.attributes,["jl_no"])??txt(sample,["jl_no","rs_jl_no","ms_jl_no"]);const upazila=txt(ctx.attributes,["upazila_ps","thana_upazila","upazila"])||txt(sample,["upazila_ps","thana_upazila","upazila"]);const district=txt(ctx.attributes,["m_district","district"])||txt(sample,["m_district","district"]);doc.setFont("helvetica","normal");doc.setFontSize(7);doc.text(`JL: ${jl||"N/A"}    District: ${district||"N/A"}    Upazila: ${upazila||"N/A"}    Plots: ${features.length}`,M,14);doc.setFontSize(6);doc.text(`Layers: ${input.layers.toUpperCase()}    CRS: EPSG:4326    Output: ${sat?"Satellite + Vector":"Vector"}`,M,19);drawScaleText(doc,e,8);drawNorthArrow(doc,W-24,M+8);drawScaleBar(doc,e,M,H-5);drawPublicationFooter(doc,{mouza,jl:jl||"N/A",upazila:upazila||"N/A",district:district||"N/A",plots:features.length,layers:input.layers.toUpperCase(),satellite:sat,scale:scale(e)});const body=Buffer.from(doc.output("arraybuffer"));return{filename:`landbd-${safe(mouza)}-${input.layers}${sat?"-satellite":""}-publication.pdf`,contentType:"application/pdf",body,meta:{mouza,width:W,height:H,zoom:0,resolution:sat?300:0,crs:"EPSG:4326 vector geometry",extent:e,tileCount:sat?1:0,plotCount:features.length,satellite:sat}};}
