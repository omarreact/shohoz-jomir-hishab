// Firebase has been replaced by the Next.js backend (SQLite via Prisma).
// This file is kept as a no-op stub so legacy imports don't break at compile time.
// Do NOT use these exports — they return empty/null objects.

export const app = null;
export const auth = null;
export const db = null;